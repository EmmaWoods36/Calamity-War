    # Calamity War Character Sprites PART_03

    Sprites originally from coding handoff PART 04 of 04.

    This zip intentionally contains only:

    assets/sprites/

    Use this as a separate sprite asset pack.

    File counts:
    - PNG sprite files: 57
    - Missing-pose marker files: 12
    - Character folders: 7

    Character folders:
    - semuda
- shani
- shanti
- shinichi
- tenganisha
- training_dummy_shadow
- yuta

    Deployment:
    Upload/merge the `assets/sprites/` folder into the GitHub repo's existing `assets/` folder.
    Do not delete unrelated assets.

    Notes:
    - `_MISSING.txt` files are placeholders showing poses that still need sprites.
    - P2 sprite mirroring should happen in code, not by creating separate left-facing files.
