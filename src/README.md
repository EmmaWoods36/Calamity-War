# Source folder

The current playable build is intentionally simple for GitHub Pages: `index.html` loads `style.css` and `game.js` directly from the repo root.

This `src/` folder is included for the next refactor pass and contains data snapshots plus notes showing where the single-file runtime can be split later.
