# UI notes

Current UI/menu rendering is in root `index.html`, `style.css`, and `game.js`. Organized UI assets are duplicated under `assets/ui/` while original flat asset paths are preserved for runtime compatibility.
